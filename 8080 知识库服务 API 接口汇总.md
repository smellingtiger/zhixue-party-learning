
​          
## 8080 知识库服务 API 接口汇总

### 接口列表

| 接口路径                        | HTTP方法 | 功能描述               |
| ------------------------------- | -------- | ---------------------- |
| `/api/files`                    | GET      | 列出所有课程文档       |
| `/api/search`                   | GET      | 全文搜索课程内容       |
| `/api/files/{doc_id}`           | GET      | 获取单个文档详情       |
| `/api/files/{doc_id}/video`     | GET      | 获取文档关联视频信息   |
| `/api/video/{course_code}`      | GET      | 视频流代理（优先缓存） |
| `/api/video/{course_code}/info` | GET      | 获取视频元信息         |
| `/api/info`                     | GET      | 服务器状态信息         |
| `/api/cache/stats`              | GET      | 视频缓存统计           |

---

### 接口详细说明

#### 1. `/api/files` - 列出课程文档

**请求参数**（Query）：
| 参数       | 类型   | 必填 | 说明                                  |
| ---------- | ------ | ---- | ------------------------------------- |
| `category` | string | 否   | 分类过滤                              |
| `q`        | string | 否   | 标题搜索关键词                        |
| `sort`     | string | 否   | 排序方式：`title`(默认)/`date`/`size` |

**响应字段**：
```json
{
  "files": [...],    // 文档列表
  "total": 2324,     // 总数
  "categories": [...], // 分类列表
  "category_counts": {} // 各分类数量
}
```

---

#### 2. `/api/search` - 全文搜索

**请求参数**（Query）：
| 参数 | 类型   | 必填 | 说明                    |
| ---- | ------ | ---- | ----------------------- |
| `q`  | string | 是   | 搜索关键词（最小长度1） |

**响应字段**：
```json
{
  "results": [
    {
      "doc_id": "xxx",
      "title": "课程标题",
      "category": "分类名",
      "snippets": [...] // 匹配片段
    }
  ],
  "total": 100
}
```

---

#### 3. `/api/files/{doc_id}` - 获取文档详情

**路径参数**：
| 参数     | 类型   | 说明   |
| -------- | ------ | ------ |
| `doc_id` | string | 文档ID |

**响应字段**：完整文档详情（包含章节、段落等）

---

#### 4. `/api/files/{doc_id}/video` - 获取视频信息

**路径参数**：
| 参数     | 类型   | 说明   |
| -------- | ------ | ------ |
| `doc_id` | string | 文档ID |

**响应字段**：
```json
{
  "has_video": true,
  "course_code": "xxx",
  "chinese_name": "课程中文名",
  "video_filename": "video.mp4",
  "video_url": "/api/video/xxx",
  "nas_path": "//nas/path/video.mp4"
}
```

---

#### 5. `/api/video/{course_code}` - 视频流

**路径参数**：
| 参数          | 类型   | 说明     |
| ------------- | ------ | -------- |
| `course_code` | string | 课程编码 |

**响应**：视频文件流（支持断点续传）

---

#### 6. `/api/video/{course_code}/info` - 视频元信息

**响应字段**：
```json
{
  "course_code": "xxx",
  "chinese_name": "课程名",
  "video_filename": "xxx.mp4",
  "file_size": 104857600,
  "file_size_display": "100.0 MB",
  "content_type": "video/mp4",
  "from_cache": true
}
```

---

#### 7. `/api/info` - 服务器状态

**响应字段**：
```json
{
  "name": "智学知识库",
  "version": "1.0.0",
  "total_files": 2324,
  "total_size": 1073741824,
  "total_size_display": "1024.0 MB",
  "categories": [...],
  "category_counts": {},
  "total_paragraphs": 48676,
  "data_source": "E:\\社院课程stt\\knowledge_base_txt",
  "processed_count": 979,
  "video_cache": {...},
  "server_time": "2026-05-25 10:30:00"
}
```

---

#### 8. `/api/cache/stats` - 缓存统计

**响应字段**：视频缓存使用情况

---

### 数据模型

**文档结构**（`to_dict()`）：
| 字段              | 类型   | 说明     |
| ----------------- | ------ | -------- |
| `id`              | string | 文档ID   |
| `title`           | string | 标题     |
| `category`        | string | 分类     |
| `size`            | int    | 文件大小 |
| `modified_time`   | str    | 修改时间 |
| `paragraph_count` | int    | 段落数   |